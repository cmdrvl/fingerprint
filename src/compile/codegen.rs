use crate::dsl::parser::FingerprintDefinition;

/// Generate Rust source implementing the Fingerprint trait from a parsed DSL definition.
pub fn generate_rust(_def: &FingerprintDefinition) -> Result<String, String> {
    todo!()
}
